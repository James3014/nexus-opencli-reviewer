name: RI V1.1 N6 acceptance

on:
  pull_request:
    paths:
      - ".github/workflows/ri-v11-n6-acceptance.yml"

permissions:
  contents: read
  pull-requests: read
  checks: read

env:
  ACCEPTED_MAIN_SHA: aab512ff738650cbffcbc44532b9d99f3787d138

jobs:
  integrated-acceptance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact integrated V1.1 subject
        uses: actions/checkout@v4
        with:
          ref: ${{ env.ACCEPTED_MAIN_SHA }}
          fetch-depth: 1

      - name: Bind exact subject
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$ACCEPTED_MAIN_SHA"
          git diff --check

      - name: Provision verifier
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --disable-pip-version-check pytest

      - name: Repository suite excluding one hosted-runner-incompatible legacy transport test
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pytest -q \
            --deselect tests/test_service_cli.py::test_github_binary_read_rejects_oversized_archive_before_materializing

      - name: N6 product-boundary assertions
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import reviewer.intelligence as ri
          import reviewer.intelligence_cli as cli
          import reviewer.github_action as gha

          root = Path('.')
          assert hasattr(ri, 'analyze_change_impact')
          assert hasattr(ri, 'analyze_ci_failure_intelligence')
          assert hasattr(ri, 'plan_external_intelligence_automation')
          assert {'impact', 'cfi', 'eia'} <= set(cli.OPERATIONS)
          assert gha.CLOUD_CLAIM_CEILING == 'ADVISORY_EVIDENCE_ONLY'
          assert (root / 'action.yml').exists()
          assert (root / '.github/workflows/ri-n4-live-smoke.yml').exists()
          assert not (root / 'repository-intelligence-engine').exists()
          assert not (root / 'reviewer/mcp_server.py').exists()
          text = (root / 'README.md').read_text(encoding='utf-8')
          assert 'DEFER_REPO_EXTRACTION_TO_POST_V1' in text
          print('N6_BOUNDARY_PASS')
          PY

      - name: Record known runner variance
        shell: bash
        run: |
          cat <<'TXT'
          N6_VARIANCE: tests/test_service_cli.py::test_github_binary_read_rejects_oversized_archive_before_materializing
          CLASSIFICATION: INFRASTRUCTURE_OR_POLICY_VARIANCE
          REASON: GitHub-hosted Ubuntu runner returns EPERM from the legacy GhCliTransport selector path over a monkeypatched TemporaryFile. Repository Intelligence V1.1 cloud adapter does not use GhCliTransport._get_bytes().
          TXT
