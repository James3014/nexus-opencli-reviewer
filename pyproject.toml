[project]
name = "nexus-opencli-reviewer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.repository-intelligence]
canonical-package = "repository-intelligence-engine"
canonical-namespace = "repository_intelligence"
canonical-version = "0.1.0"
vendored-wheel = "vendor/repository_intelligence_engine-0.1.0-py3-none-any.whl"
legacy-namespace = "reviewer.intelligence"
legacy-mode = "forwarding-only"

[tool.pytest.ini_options]
pythonpath = ["."]
